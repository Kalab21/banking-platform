# Normalise line endings so the repo is consistent across Windows/Linux/macOS.
* text=auto eol=lf

# Windows-only scripts keep CRLF
*.ps1   text eol=crlf
*.cmd   text eol=crlf
*.bat   text eol=crlf

# Binary
*.jar   binary
*.png   binary
*.jpg   binary
*.pdf   binary
