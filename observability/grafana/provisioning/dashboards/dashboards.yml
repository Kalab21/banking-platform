apiVersion: 1

providers:
  - name: banking-platform
    orgId: 1
    folder: Banking Platform
    type: file
    disableDeletion: false
    # Picked up from disk on startup; no import step, no dashboard IDs to
    # remember.
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
